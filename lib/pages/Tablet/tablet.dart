import 'package:flutter/material.dart';

class Tablet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(
        child: Text("Tablet",textScaleFactor: 2.5,),
      ),
    );
  }
}